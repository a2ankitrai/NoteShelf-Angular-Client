import { TitleCaserPipe } from './title-caser.pipe';

describe('TitleCaserPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaserPipe();
    expect(pipe).toBeTruthy();
  });
});
